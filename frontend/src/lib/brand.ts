// The product name, kept in one place so the header, auth card, and any future
// brand surface stay in step. The document <title> lives in index.html.
export const APP_NAME = 'Ma Garde Sereine'

// The production origin, used to build absolute canonical/og:url values at
// runtime (Open Graph requires absolute URLs). Keep this in step with the same
// host hardcoded in index.html, robots.txt and sitemap.xml.
export const SITE_URL = 'https://ma-garde-sereine.fr'
